spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:16.3-alpine:///
logging:
  level:
    org:
      springframework:
        jdbc: TRACE
        transaction: TRACE
